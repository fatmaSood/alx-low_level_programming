#include "main.h"
/**
 * main function that prints the alphabet, in lowercase, followed by a new line.
 * Return: Always 0.
 */
void alphabet(void)
{
char ch;
	ch = 'a';
	while (ch <= 'z')
	{
	_putchar(ch);
		ch++;
	
	}
}
	int main()
	{
	_putchar('\n');
		return (0);
	}
